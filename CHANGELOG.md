# Changelog

## 0.1.0 — 2026-07-27
- Release candidate scaffolding: Electron + Python sidecar, data in OS `userData`.
- Onboarding při prvním startu (`planer_onboarded`).
- Simulation mode označen jako experimental / beta.
- `schemaVersion` v serializaci projektů (`Project.js`).
- API: `/api/ping`, `PORT` / `PLANER_DATA_DIR` / `PLANER_STATIC_DIR` env.
- Toast při chybách uložení / uploadu (400, 413).
- Ctrl+S ukládá projekt.
- Sticky notes, notebook, timeline, master doc (stávající funkce v balíčku v1).

## Unreleased
- (prázdné)
